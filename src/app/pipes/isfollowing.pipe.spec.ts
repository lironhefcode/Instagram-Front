import { IsfollowingPipe } from './isfollowing.pipe';

describe('IsfollowingPipe', () => {
  it('create an instance', () => {
    const pipe = new IsfollowingPipe();
    expect(pipe).toBeTruthy();
  });
});
